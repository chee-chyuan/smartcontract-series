# setting up first hardhat project
- basic js setup by following hardhat tutorial
- testing out npx hardhat node and see that private keys are returned in this js setup (doesnt seemed to work in typescript)